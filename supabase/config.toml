project_id = "wegphblwwcfidvdbdtdq"

# Estas funciones no usan el JWT del gateway porque son tareas internas de
# pg_cron. Ambas rechazan toda solicitud cuyo x-cron-secret no coincida con el
# secreto privado almacenado en Vault.
[functions.detectar-reportes-pendientes]
verify_jwt = false

[functions.despachar-informes]
verify_jwt = false

# Formulario público de contacto: aplica validación de origen, honeypot y
# control de frecuencia dentro de la propia función.
[functions.contacto-publico]
verify_jwt = false

# Portal público de acreditaciones: autentica dentro de la función mediante
# token único, clave corporativa y control de frecuencia.
[functions.acreditacion-publica]
verify_jwt = false
