import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.57.4";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,"Content-Type":"application/json"}});
const schema={type:"object",additionalProperties:false,properties:{resultado:{type:"string",enum:["Conforme","Observado","No legible"]},confianza:{type:"number"},resumen:{type:"string"},nombre_documento:{type:"string"},titular_nombre:{type:"string"},titular_rut:{type:"string"},fecha_emision:{type:"string"},fecha_vencimiento:{type:"string"},entidad_emisora:{type:"string"},requiere_revision:{type:"boolean"},advertencias:{type:"array",items:{type:"string"}}},required:["resultado","confianza","resumen","nombre_documento","titular_nombre","titular_rut","fecha_emision","fecha_vencimiento","entidad_emisora","requiere_revision","advertencias"]};
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});const started=Date.now();let reservationId:string|undefined;let db:any;try{const serviceKey=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY"),openAIKey=Deno.env.get("OPENAI_API_KEY");if(!serviceKey||!openAIKey)return json({error:"La revisión inteligente no está configurada."},503);db=createClient(Deno.env.get("SUPABASE_URL")!,serviceKey,{auth:{persistSession:false}});const token=(req.headers.get("Authorization")||"").replace(/^Bearer\s+/i,"");const{data:authData,error:authError}=await db.auth.getUser(token);if(authError||!authData?.user)return json({error:"Debes iniciar sesión."},401);const body=await req.json();const{file_base64,mime_type,file_name,empresa:requestedCompany,tipo_documento,trabajador_nombre,trabajador_rut}=body||{};if(!file_base64||!["application/pdf","image/jpeg","image/png","image/webp"].includes(mime_type))return json({error:"Adjunta PDF, JPG, PNG o WEBP."},400);let profileQuery=db.from("usuarios").select("usuario,nombre,correo,empresa").eq("auth_user_id",authData.user.id);if(requestedCompany)profileQuery=profileQuery.eq("empresa",requestedCompany);const{data:profiles,error:profileError}=await profileQuery.limit(2);if(profileError)throw profileError;if(!profiles?.length)return json({error:"Cuenta no autorizada para esta empresa."},403);const profile=profiles[0];const[{data:globalConfig},{data:companyConfig}]=await Promise.all([db.from("config_global_obraxis").select("ia_habilitada,ia_modelo,ia_archivo_max_mb").eq("id",1).maybeSingle(),db.from("ia_config_empresas").select("*").eq("empresa",profile.empresa).maybeSingle()]);if(globalConfig?.ia_habilitada===false)return json({error:"La IA está deshabilitada globalmente."},503);const bytes=Math.ceil(String(file_base64).length*.75),maxMb=Number(globalConfig?.ia_archivo_max_mb||10);if(bytes>maxMb*1024*1024)return json({error:`El archivo supera ${maxMb} MB.`},413);const model=companyConfig?.modelo||globalConfig?.ia_modelo||"gpt-4.1-mini";const{data:reserved,error:reserveError}=await db.rpc("ia_reservar_consumo",{p_empresa:profile.empresa,p_obra_nombre:"",p_auth_user_id:authData.user.id,p_usuario:profile.nombre||profile.usuario||profile.correo,p_funcion:"rrhh",p_modelo:model,p_reserva_usd:.05});if(reserveError)return json({error:reserveError.message},403);reservationId=reserved;const prompt=`Extrae y revisa este documento laboral chileno como apoyo administrativo, sin emitir una conclusión legal. Tipo esperado: ${tipo_documento}. Trabajador esperado: ${trabajador_nombre}, RUT ${trabajador_rut||"no informado"}. Comprueba correspondencia visible de nombre y RUT, tipo documental, legibilidad, entidad emisora, fecha de emisión y vencimiento. Fechas YYYY-MM-DD; si no existen usa cadena vacía. Marca requiere_revision cuando haya discrepancias, datos esenciales ilegibles, documento vencido o no corresponda al tipo esperado. No inventes datos y enumera advertencias concretas.`;const filePart=mime_type.startsWith("image/")?{type:"input_image",image_url:`data:${mime_type};base64,${file_base64}`,detail:"high"}:{type:"input_file",filename:file_name||"documento.pdf",file_data:`data:${mime_type};base64,${file_base64}`};const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${openAIKey}`,"Content-Type":"application/json"},body:JSON.stringify({model,input:[{role:"user",content:[{type:"input_text",text:prompt},filePart]}],text:{format:{type:"json_schema",name:"revision_documento_rrhh",strict:true,schema}}})});const apiData=await response.json();if(!response.ok)throw new Error(apiData?.error?.message||"No fue posible revisar el documento.");const raw=apiData.output_text||apiData.output?.flatMap((x:any)=>x.content||[]).find((x:any)=>x.type==="output_text")?.text;if(!raw)throw new Error("La IA no devolvió datos utilizables.");const result=JSON.parse(raw);const inputTokens=Number(apiData.usage?.input_tokens||0),outputTokens=Number(apiData.usage?.output_tokens||0),cost=(inputTokens*.40+outputTokens*1.60)/1_000_000;await db.rpc("ia_finalizar_consumo",{p_id:reservationId,p_estado:"Completado",p_tokens_entrada:inputTokens,p_tokens_salida:outputTokens,p_costo_usd:cost,p_confianza:Number(result.confianza||0),p_duracion_ms:Date.now()-started,p_error_detalle:"",p_metadatos:{mime_type,bytes,tipo_documento,trabajador_rut}});return json({data:result,usage:{tokens_total:inputTokens+outputTokens,costo_usd:cost}})}catch(error){if(reservationId&&db)await db.rpc("ia_finalizar_consumo",{p_id:reservationId,p_estado:"Error",p_tokens_entrada:0,p_tokens_salida:0,p_costo_usd:0,p_confianza:null,p_duracion_ms:Date.now()-started,p_error_detalle:error instanceof Error?error.message:String(error),p_metadatos:{}});return json({error:error instanceof Error?error.message:String(error)},400)}});
